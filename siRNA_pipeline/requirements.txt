# ================================================================
# Python 依赖清单（siRNA_pipeline）
# Python 版本：>= 3.9（建议 3.10-3.13；已在 Python 3.13.0 验证）
# 平台：Windows / Linux 均可，无需 GPU
# 完整管线组件（ViennaRNA / BLAST+ / RNA-FM / OligoFormer / PITA-TargetScan）
# 的安装方式与配置见 环境要求.txt
# ----------------------------------------------------------------
# 主管道必需（predict.py 六步流水线 + 单元测试）：
torch==2.14.0
PyYAML==6.0.3
numpy==2.4.3
pandas==3.0.1
# 复现答辩图/分析图（scripts/make_*_figures.py）需要：
matplotlib==3.11.2
# 训练/评估入口（train.py：C_match 错配模型 Ridge/Siamese 训练与评估）需要：
scikit-learn==1.9.1

# ----------------------------------------------------------------
# OligoFormer（完整管线必需组件）：使用其自带虚拟环境
# OligoFormer部分/.venv（Python 3.8-3.10），官方依赖与其 requirements.txt 一致。
# 注意：torch 版本与主管道不同（2.2.1），必须装在 .venv 中，勿与主管道混装。
# 在 OligoFormer部分/.venv 内安装以下 13 项：
#   torch==2.2.1
#   torchvision==0.17.1
#   bio==1.6.2
#   matplotlib==3.7.5
#   numpy==1.24.4
#   pandas==2.0.3
#   prefetch-generator==1.0.3
#   ptflops==0.7.3
#   pytorch-ignite==0.5.0.post2
#   scikit-learn==1.3.1
#   scipy==1.10.1
#   tqdm==4.66.1
#   yacs==0.1.8
